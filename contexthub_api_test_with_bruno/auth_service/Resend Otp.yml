info:
  name: Resend Otp
  type: http
  seq: 4

http:
  method: POST
  url: "{{auth_service_base_url}}/auth/resend_otp"
  body:
    type: json
    data: |-
      {
        "id":2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
