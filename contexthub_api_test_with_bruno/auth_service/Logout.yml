info:
  name: Logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{auth_service_base_url}}/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
