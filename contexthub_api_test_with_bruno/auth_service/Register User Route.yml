info:
  name: Register User Route
  type: http
  seq: 2

http:
  method: POST
  url: "{{auth_service_base_url}}/auth/register"
  body:
    type: json
    data: |-
      {
        "first_name":"Maruti",
        "last_name":"Bandagar",
        "email":"maruti.bandagar@gmail.com",
        "password":"pass123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
