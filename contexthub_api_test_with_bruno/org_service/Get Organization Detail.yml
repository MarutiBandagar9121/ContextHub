info:
  name: Get Organization Detail
  type: http
  seq: 5

http:
  method: GET
  url: "{{org_service_base_url}}/organization/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
