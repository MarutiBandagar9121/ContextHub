info:
  name: Get All Org of User
  type: http
  seq: 3

http:
  method: GET
  url: "{{org_service_base_url}}/organization"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
