info:
  name: Helath Check
  type: http
  seq: 1

http:
  method: GET
  url: "{{org_service_base_url}}/organization/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
