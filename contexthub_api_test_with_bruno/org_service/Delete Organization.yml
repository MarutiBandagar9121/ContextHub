info:
  name: Delete Organization
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{org_service_base_url}}/organization/1"
  body:
    type: json
    data: |-
      {
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
